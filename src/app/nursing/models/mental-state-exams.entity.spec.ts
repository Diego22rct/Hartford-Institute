import { MentalStateExamsEntity } from './mental-state-exams.entity';

describe('MentalStateExamsEntity', () => {
  it('should create an instance', () => {
    expect(new MentalStateExamsEntity()).toBeTruthy();
  });
});
